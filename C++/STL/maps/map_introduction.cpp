/*
Maps are associative containers that store elements in a mapped fashion. Each element has a key value and a mapped value.

No two mapped values can have same key values.

maps store data into ascending oreder/lexalogicall based on key

maps implement with red-black tress internally
unordered maps implement with hash table inernully

we can use any complex datatype with maps but we can not use it with unordered maps

Functions : 

*/
